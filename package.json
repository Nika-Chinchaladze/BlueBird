{
  "name": "bluebird",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test:bluebird": "npx playwright test --grep @bluebird",
    "test:bluebird-ui": "npx playwright test --grep @bluebird --ui",
    "html:report": "npx playwright show-report",
    "html:clean": "rd /s /q playwright-report && rd /s /q test-results"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.51.1",
    "@types/node": "^22.14.0"
  }
}
